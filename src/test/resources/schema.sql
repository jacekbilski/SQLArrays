CREATE TABLE IF NOT EXISTS entity_with_list (id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, list TEXT ARRAY[5]);

CREATE TABLE IF NOT EXISTS entity_with_array (id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, arr TEXT ARRAY[5]);

CREATE TABLE IF NOT EXISTS pure_jdbc (id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, arr TEXT ARRAY[5]);
