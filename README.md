### Playing around with SQL Arrays

Small project to see how to deal with SQL arrays using three approaches:
* pure SQL (using NamedParameterJdbcTemplate),
* Spring Data
* JPA

Works with PostgreSQL 14.
